import { ImgUploadDirective } from './img-upload.directive';

describe('ImgUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgUploadDirective();
    expect(directive).toBeTruthy();
  });
});
